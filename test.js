const { describe } = require("mocha");

var expect = chai.expect;

describe(`${checkingscores}`, chekingscores() {
    describe('ifequal', ifqual() {
        it('should check scores of each player', chekingscores() {
            var x = ifqual('Tie');
            expect(x).to.equal('its a tie');
        });
        
    });
});
